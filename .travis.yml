language: ruby
cache: bundler

sudo: false

rvm:
  - 3.4
  - 3.3
  - 3.2

gemfile:
  - gemfiles/activerecord_7.1.5.gemfile
  - gemfiles/sinatra_4.2.gemfile

before_install:
  - gem update --system

before_script:
  - bundle exec rake -f spec/dummy/Rakefile db:schema:load
